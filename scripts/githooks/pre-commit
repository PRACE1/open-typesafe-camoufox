#!/bin/sh
# Installed to .git/hooks/pre-commit by scripts/install-hooks.ps1.
# Advisory file map via Jev; never blocks unless --strict is added here.
# Bypass: JEV_HOOKS_OFF=1 git commit
command -v uv >/dev/null 2>&1 || exit 0
exec uv run python scripts/jev_hooks.py pre-commit "$@"
